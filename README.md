# Multi-Agent Research Assistant

An advanced AI project demonstrating a multi-agent workflow using LangGraph concepts. 
Different agents (Searcher, Writer, Critic) collaborate to generate high-quality research reports.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Set API Key: `set GEMINI_API_KEY=your_key`
3. Run: `python main.py`
